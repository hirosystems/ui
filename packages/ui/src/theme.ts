export {
  baseSizes,
  colors,
  namedSpacingUnits,
  shadows,
  sizes,
  space,
  textStyles,
  theme,
  themeColor,
  transition,
  typography,
} from '@stacks/ui-theme';

export type { Colors, Spacing, TextStyles } from '@stacks/ui-theme';
