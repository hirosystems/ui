export {
  baseSizes,
  colors,
  namedSpacingUnits,
  shadows,
  sizes,
  textStyles,
  theme,
  transition,
  typography,
} from '@stacks/ui-theme';

export type { Colors, Spacing, TextStyles } from '@stacks/ui-theme';
